import styled from "styled-components";

function MainPage() {
    return (
        <PageWrapper>
            
        </PageWrapper>
    )
}

export default MainPage;

// CSS
const PageWrapper = styled.div`
    color: white;
`;